To view these sample web pages, first import the code into VS Code. 
Open an integrated terminal/shell window in VS Code and install the 
packages (dependencies) the samples require (see package.json), as follows:

    $ npm install

Assuming the Live Server extension is installed in VS Code, start the server. 
In the browser, click a sample page to trigger its related React code.